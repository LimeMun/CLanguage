#include <stdio.h>

int main(void){
	char c = 'A';
	int i = 3, j = 3, k = 3, m = -3;
	double x = 0.1, y = 2.3;
	
	printf(" -5 < m && m < -1: %d\n", -5 < m && m < -1);
	printf(" -5 < m < -1: %d\n", -5 < m < -1);

	return 0;
}

